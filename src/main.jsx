import React from 'react'
import ReactDOM from 'react-dom/client'
import WalPoll from './WalPoll'
ReactDOM.createRoot(document.getElementById('root')).render(<React.StrictMode><WalPoll /></React.StrictMode>)
